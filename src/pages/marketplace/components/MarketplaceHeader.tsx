
import React from 'react';
import AppHeader from '@/components/AppHeader';

const MarketplaceHeader = () => {
  return (
    <AppHeader variant="marketplace" showLocationPin={true} />
  );
};

export default MarketplaceHeader;
